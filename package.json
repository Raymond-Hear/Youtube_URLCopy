{
  "name": "youtube-url-copy-edge-extension",
  "version": "1.5.0",
  "private": true,
  "description": "可按仅链接或标题加链接格式分批复制 YouTube 视频的 Edge 扩展",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:ui": "python tests/ui-playwright-check.py",
    "check": "node --check src/shared.js && node --check src/youtube-data.js && node --check src/background.js && node --check src/collector.js"
  }
}
